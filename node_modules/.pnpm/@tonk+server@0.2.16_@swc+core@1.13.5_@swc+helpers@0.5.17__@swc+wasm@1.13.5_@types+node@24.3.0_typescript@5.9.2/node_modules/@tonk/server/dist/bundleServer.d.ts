import { BundleServerConfig } from './types.js';
export declare class BundleServer {
    private app;
    private server;
    private config;
    private isRunning;
    private startTime?;
    private rootNode;
    constructor(config: BundleServerConfig);
    private log;
    private setupApiProxies;
    private setupRoutes;
    updateWasmCache(): Promise<void>;
    start(): Promise<void>;
    stop(): Promise<void>;
    getStatus(): {
        isRunning: boolean;
        port: number;
        bundleName: string;
        startTime: Date | undefined;
    };
}
//# sourceMappingURL=bundleServer.d.ts.map