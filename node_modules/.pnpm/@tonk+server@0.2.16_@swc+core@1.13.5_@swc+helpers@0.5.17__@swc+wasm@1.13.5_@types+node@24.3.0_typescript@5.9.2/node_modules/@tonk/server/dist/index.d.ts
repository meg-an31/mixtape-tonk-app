export interface ServerOptions {
    port?: number;
    persistencePath?: string;
    storesPath?: string;
    bundlesPath?: string;
    verbose?: boolean;
    syncInterval?: number;
}
export declare class TonkServer {
    private app;
    private socket;
    private server;
    private readyResolvers;
    private options;
    private repo;
    private fsSyncTimer;
    private bundleServers;
    private bundleRoutes;
    private upload;
    private rootNode;
    private bundlePersistence;
    constructor(options: ServerOptions);
    private initRoot;
    private restorePersistedBundles;
    private setupDirectories;
    private log;
    private setupExpressMiddleware;
    private setupBundleRoute;
    private setupBundleApiProxies;
    start(): Promise<void>;
    stop(): Promise<void>;
}
export declare function createServer(options: ServerOptions): Promise<TonkServer>;
//# sourceMappingURL=index.d.ts.map