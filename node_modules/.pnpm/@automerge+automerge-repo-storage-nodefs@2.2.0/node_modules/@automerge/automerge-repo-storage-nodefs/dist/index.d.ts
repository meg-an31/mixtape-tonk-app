/**
 * @packageDocumentation
 * A `StorageAdapter` which stores data in the local filesystem
 */
import { Chunk, StorageAdapterInterface, type StorageKey } from "@automerge/automerge-repo/slim";
export declare class NodeFSStorageAdapter implements StorageAdapterInterface {
    private baseDirectory;
    private cache;
    /**
     * @param baseDirectory - The path to the directory to store data in. Defaults to "./automerge-repo-data".
     */
    constructor(baseDirectory?: string);
    load(keyArray: StorageKey): Promise<Uint8Array | undefined>;
    save(keyArray: StorageKey, binary: Uint8Array): Promise<void>;
    remove(keyArray: string[]): Promise<void>;
    loadRange(keyPrefix: StorageKey): Promise<Chunk[]>;
    removeRange(keyPrefix: string[]): Promise<void>;
    private cachedKeys;
    private getFilePath;
}
//# sourceMappingURL=index.d.ts.map